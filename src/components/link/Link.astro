---
import type { HTMLAttributes } from "astro/types"

interface Props extends HTMLAttributes<"a"> {}

const { href, ...attrs } = Astro.props

const base = import.meta.env.BASE_URL.replace(/\/$/, "")
---

<a href={base + href} {...attrs}>
    <slot />
</a>
