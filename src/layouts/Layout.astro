---
import "@fontsource/merriweather"
import NavigationBar from "../components/navbar/NavigationBar.astro"
import Logo from "../components/navbar/Logo.astro"
import NavItem from "../components/navbar/NavItem.astro"
import "../styles/variables.scss"
import {FaFacebookF} from "@react-icons/all-files/fa/FaFacebookF"
import {BsInstagram} from "@react-icons/all-files/bs/BsInstagram"

// const FaFacebookF = fa.FaFacebookF
// const BsInstagram = bs.BsInstagram
export interface Props {
    title: string
}

const { title } = Astro.props

const fullTitle = title
    ? `${title} | Saffron Murray-Browne`
    : `Saffron Murray-Browne`
---

<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content="Astro description" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>{fullTitle}</title>
    </head>
    <body>
        <NavigationBar>
            <Logo slot="Logo" text="Saffron Murray-Browne" />
            <NavItem
                slot="NavItem"
                href="/"
                text="Portfolio"
                additionalUnderlinePaths={[
                    "/portfolio/paintings",
                    "/portfolio/drawings",
                ]}
            />
            <NavItem slot="NavItem" href="/about" text="About" />
            <NavItem slot="NavItem" href="/shop" text="Shop" />
            <div class="navIcons">
                <BsInstagram />
                <FaFacebookF />
            </div>
        </NavigationBar>
        <slot />
    </body>
</html>

<style is:global lang="scss">
    @import "../styles/variables.scss";

    .navIcons {
        display: flex;
        padding-top: 8px;
    }
    .navIcons > svg {
        width: 1.2em;
        height: 1.2em;
        padding-left: 32px;
    }

    html,
    body {
        width: 100%;
        // height: 100%;
        /* box-sizing: content-box; */
        margin: 0;
        padding: 0;
        font-family: merriweather, arial;
        line-height: 2.2rem;
        overflow-x: hidden;
    }
    body {
        width: 100%;
        box-sizing: border-box;
        // height: 100%;
        // margin: 0;
        padding: 0 48px 48px;

        @media (max-width: $breakWidth) {
            padding: 32px;
        }
    }

    a {
        color: $linkColor;
    }

    /* My CSS */
</style>
